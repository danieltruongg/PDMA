import { KilogramGramPipe } from './kilogram-gram.pipe';

describe('KilogramGramPipe', () => {
  it('create an instance', () => {
    const pipe = new KilogramGramPipe();
    expect(pipe).toBeTruthy();
  });
});
